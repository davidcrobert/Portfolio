export const config = {
    noiseOverlay: {
      opacity: 1, // Increased to make the effect more visible
      zIndex: 9999,
      staticIntensity: 0.5,
      scanlineIntensity: 0.05,
      flickerProbability: 0.95,
      flickerIntensity: 0.7,
      colorShiftIntensity: 0.05,
    },
    // Add other global configuration settings here
  };